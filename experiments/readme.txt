This directory will contain csv results files for experiments run with our runtime system. The results files will be named according to the following convention:

```<timestamp>_<function>_<balancing_policy>_<ndp_enabled>_<num_iterations>```